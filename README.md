# Blender-fence-Generator
This repository contains a Blender Python (bpy) script that generates a 3D fence model. The script creates a long cylinder with a sharp cone on top, joins them into one object, and duplicates the unit into 8 neatly aligned fence segments using procedural automation.
